/**
 * Optional EmailJS — leave YOUR_* placeholders to use FormSubmit instead.
 * FormSubmit already sends every application to workschengenhr@gmail.com on submit.
 * When you add real EmailJS keys below, submissions switch to EmailJS automatically.
 */
window.EMAILJS_CONFIG = {
  publicKey: 'YOUR_PUBLIC_KEY',
  serviceId: 'YOUR_SERVICE_ID',
  templateId: 'YOUR_TEMPLATE_ID',
};
